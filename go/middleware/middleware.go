// Package middleware contains middleware code that runs before the bussiness logic of each route
package middleware

import "github.com/VinukaThejana/link-shortner/backend/schemas"

type response = schemas.Response
